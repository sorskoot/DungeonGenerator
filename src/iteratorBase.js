import { NodeBase } from "./nodeBase.js";

export class IteratorBase extends NodeBase {
    /**
     * execute gets called for every container(room) in the dungeon
     * @param {*} currentContainer 
     * @param {*} index 
     */
    execute(currentContainer, index) {
    }
}


