{
  "name": "example",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "esbuild ./js/index.js --sourcemap --bundle --outfile=\"deploy/index.js\"",
    "build:watch": "esbuild ./js/index.js --sourcemap --bundle --outfile=\"deploy/index.js\" --watch --servedir=."
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.19.11"
  },
  "dependencies": {
    "dungeon-generator": "file:.."
  }
}
